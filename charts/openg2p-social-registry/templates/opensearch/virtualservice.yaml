{{- if .Values.opensearch.enabled }}
{{- if .Values.opensearch.istio.enabled }}
{{- if .Values.opensearch.istio.virtualservice.enabled }}
{{- $istioVsHostname := include "common.tplvalues.render" (dict "value" .Values.opensearch.istio.virtualservice.host "context" .Subcharts.opensearch) }}
{{- $mainHostname := include "common.tplvalues.render" (dict "value" .Values.opensearch.hostname "context" .Subcharts.opensearch) }}
{{- $hostname := default $mainHostname $istioVsHostname }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "common.names.fullname" .Subcharts.opensearch }}
  labels: {{- include "common.labels.standard" (dict "customLabels" .Values.opensearch.commonLabels "context" .Subcharts.opensearch) | nindent 4 }}
  {{- if .Values.opensearch.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.opensearch.commonAnnotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  hosts:
  - {{ $hostname | quote }}
  gateways:
  - {{ default (include "common.names.fullname" .Subcharts.opensearch) .Values.opensearch.istio.virtualservice.gateway }}
  http:
  - headers:
      request:
        set:
          x-forwarded-host: {{ $hostname | quote }}
          x-forwarded-proto: https
    route:
    - destination:
        host: {{ include "common.tplvalues.render" (dict "value" .Values.opensearch.istio.virtualservice.destination "context" .Subcharts.opensearch) }}
        port:
          number: {{ include "common.tplvalues.render" (dict "value" .Values.opensearch.istio.virtualservice.destinationPort "context" .Subcharts.opensearch) }}
{{- end }}
{{- end }}
{{- end }}
